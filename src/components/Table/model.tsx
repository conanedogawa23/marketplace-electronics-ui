export interface RowData {
    id: number;
    location: string;
    project: string;
    quantity: number;
    description: string;
}

export interface HeadCell {
    id: string;
    label: string;
}
